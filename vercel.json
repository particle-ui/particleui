{
  "buildCommand": "pnpm --filter tokens build && node packages/registry-builder/build.mjs && node packages/registry-builder/build-index.mjs && pnpm --filter web build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs"
}
