{
  "name": "@particleui/figma-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Figma plugin — match selected frames to ParticleUI components",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.95.0",
    "esbuild": "^0.21.0",
    "typescript": "^5.4.0"
  }
}
