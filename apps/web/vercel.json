{
  "buildCommand": "cd ../.. && pnpm --filter tokens build && pnpm --filter registry-builder exec node build.mjs && node packages/registry-builder/build-index.mjs && pnpm --filter web build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "env": {
    "NEXT_TELEMETRY_DISABLED": "1"
  }
}
