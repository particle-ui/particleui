{
  "name": "@particleui/mcp",
  "version": "0.1.0",
  "description": "ParticleUI MCP server — lets Claude Code search, install, and configure ParticleUI components",
  "type": "module",
  "bin": {
    "particleui-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.25.30"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "@types/node": "^22.15.21"
  }
}
